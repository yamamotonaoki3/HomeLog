{
  "name": "homelog-backend-workers",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:dry-run": "wrangler deploy --dry-run",
    "test": "npm run wrangler-types && vitest run",
    "wrangler-types": "wrangler types",
    "typecheck": "npm run wrangler-types && tsc --noEmit",
    "lint": "eslint ."
  },
  "dependencies": {
    "drizzle-orm": "^0.45.3",
    "hono": "^4.13.8",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.22.0",
    "@types/node": "^22.20.4",
    "@typescript-eslint/eslint-plugin": "^8.70.1",
    "@typescript-eslint/parser": "^8.0.0",
    "drizzle-kit": "^0.31.11",
    "eslint": "^9.0.0",
    "typescript": "^5.7.0",
    "vitest": "^4.1.0",
    "wrangler": "^4.136.1"
  }
}
